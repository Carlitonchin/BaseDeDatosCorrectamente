@model IEnumerable<BaseDatosFisica.Models.ParaLasVistas.TestCurso_Pregunta>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.respuesta)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.respuesta)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TestCursoPreguntaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TestCursoPreguntaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TestCursoPreguntaID })
        </td>
    </tr>
}

</table>
