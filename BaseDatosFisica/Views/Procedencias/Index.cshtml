@model IEnumerable<BaseDatosFisica.Models.Procedencia>

@{
    ViewBag.Title = "Procedencia";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.valor)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.valor)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProcedenciaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProcedenciaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProcedenciaID })
        </td>
    </tr>
}

</table>
